<div class="comment clearfix">
   <div class="avatar">
     <%= link_to  profile_path(comment.user.name) do %>
       <img src=<%= comment.user.avatar %> alt="" class="image-circle">
     <% end %>
   </div>
   <div class="body">
     <div class="heading">
        <h5 class="name"><%= link_to comment.user.name, profile_path(comment.user.name) %></h5>
        <span class="datetime">
          <%= time_ago_in_words comment.created_at %>前
        </span>
     </div>
     <%= markdown comment.content %>
   </div>
</div>
