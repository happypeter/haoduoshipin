$(".comments").append("<%= j render :partial => 'comments/editable_comment', :locals => {comment: @comment} %>");
$('#new_comment textarea').val("");
commentInt(); // need to recall the client side code
